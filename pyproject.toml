[build-system]
requires = ["maturin>=1.9,<2"]
build-backend = "maturin"

[project]
name = "pydocfix"
version = "0.1.0b2"
description = "A Rust-powered Python docstring linter with auto-fix support"
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
authors = [
    { name = "Ryuma Asai" },
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Rust",
    "Topic :: Software Development :: Quality Assurance",
]
dependencies = []

[project.urls]
Homepage = "https://github.com/ryumasai/pydocfix"
Repository = "https://github.com/ryumasai/pydocfix"
Issues = "https://github.com/ryumasai/pydocfix/issues"
Changelog = "https://github.com/ryumasai/pydocfix/releases"

[dependency-groups]
dev = [
    "maturin>=1.9,<2",
    "pydoclint>=0.8.3",
    "ruff>=0.11",
    "pre-commit>=4.5.1",
]

[tool.ruff]
target-version = "py311"
line-length = 120
extend-include = ["tests/**/*.py"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "B", "A", "SIM"]

[tool.maturin]
bindings = "bin"
manifest-path = "crates/pydocfix-cli/Cargo.toml"
strip = true

[tool.pydocfix]
exclude = ["tests/**/fixtures/"]
